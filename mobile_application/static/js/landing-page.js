if ($(window).width() < 400) {
  $('.brand-name').css('font-size', '5px')
}

// $('.loader').fadeIn('slow')

$(document).ready(() => {
//   $('.loader').fadeOut('slow')

  //   $('#home-page-btn').click(() => {

  //   })

  // window.location = window.sessionStorage.getItem('prevRoute')
})
